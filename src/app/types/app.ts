import { Component } from '@angular/core';
import { Directive } from '@angular/core'

export class App {
    name : string;
    component : Object;
    imgurl: string;
    description: string;
    url: string;
}